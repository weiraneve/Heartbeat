# Uncomment the next line to define a global platform for your project

target 'heartbeat_iOS' do
platform :ios, '13.0'
  

  # Pods for heartbeat_iOS
  
end

target 'heartbeat_macOS' do
platform :osx, '11.0'
  

  # Pods for heartbeat_macOS
  
end
